# Freight Rate Prediction - runtime dependencies
#
# Phase-3 audit finding C-2: the original file listed only the three packages
# required by the provided `score.py`. Every modelling and preprocessing module
# in `src/` imports scikit-learn, and every generated report calls
# DataFrame.to_markdown() which requires `tabulate`. Installing the original
# file therefore produced a non-functional environment.

# --- Core data stack (also required by the provided score.py) ----------------
matplotlib>=3.8,<4
numpy>=1.26,<3
pandas>=2.0,<3

# --- Preprocessing / modelling pipeline --------------------------------------
scikit-learn>=1.4,<2
joblib>=1.3,<2

# --- Configuration -----------------------------------------------------------
PyYAML>=6.0,<7

# --- Gradient boosting libraries (Phase 5) -----------------------------------
# Optional at runtime: src/advanced_models.py skips any that are missing rather
# than failing, so the pipeline still runs on a scikit-learn-only environment.
xgboost>=2.0,<4
lightgbm>=4.0,<5
catboost>=1.2,<2

# --- Report generation (DataFrame.to_markdown) -------------------------------
tabulate>=0.9,<1

# --- Testing -----------------------------------------------------------------
pytest>=8.0,<9
